server:
    port: 9000

spring:
    datasource: #数据库源
        username: root
        password: cjm520
        url: jdbc:mysql://localhost:3306/sport?useUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=UTC
        driver-class-name: com.mysql.cj.jdbc.Driver

#数据库配置
mybatis:
  mapper-locations: classpath:mapping/*Mapping.xml
  type-aliases-package: com.chenjm.sportplay.bean

#showSql
logging:
  level:
    com:
      chenjm: debug